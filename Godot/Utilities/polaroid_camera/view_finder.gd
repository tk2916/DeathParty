extends TextureRect
#
#@export var viewfinder_camera: Camera2D
#
#function for camera movement, what the player sees 
#
#func _ready():
	#viewfinder_camera.make_current()

#func _physics_process(delta):
	#camera stays on the polaroid camera image wih some adjustments 
	#viewfinder_camera.position = position + Vector2(-210, 80)
	#if	viewfinder_camera.position.x < 20:
		#viewfinder_camera.position.x=20
	#
	#if	viewfinder_camera.position.x > 133:
		#viewfinder_camera.position.x= 133
		#
	#if	viewfinder_camera.position.y > 140:
		#viewfinder_camera.position.y=140
		
